import React from 'react';
import { StyledLoaderWrapper, StyledLoader } from './StyledLoader';

const Loader = () => {
  return (
    <StyledLoaderWrapper>
      <StyledLoader />
    </StyledLoaderWrapper>
  );
};

export default Loader;
